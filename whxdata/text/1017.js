rh._.exports({"1":["T010: Initial Mesh Refinement Technique \r\n \r\nIntroduction \r\nThis document describes a meshing technique that may be needed when using \r\nwindows to create an initial mesh for an object in DEFORM-3D. \r\nProblem Description \r\nMesh windows can be used to generate a refined mesh at certain locations on a \r\npart.  There are times when mesh windows are defined, but the resulting mesh \r\ndoes not look like the windows were taken into account.  This is due to the way \r\nthe mesh windows use the underlying geometry.  This technical note will show \r\nhow different geometries for the same object will change how the mesh windows \r\nare used. \r\nBackground Information \r\nThe geometries for a simple block and cylinder are shown below.  When a \r\nstandard .stl file is generated for these objects, the geometry uses the fewest \r\nnumber of triangles  to represent the object.  This is why the triangles on the \r\nobjects stretch from one side to the other. \r\n \r\n----------------Page (0) Break----------------\r\nIf you want to use windows to refine the mesh in the center of these objects, you \r\nmay run into difficulty.  This is because the mesh windows act at the corners of \r\nthe geometry facets.  Due to the discretization of the geometries, there are no \r\nfacet corners in the center of the objects.  Mesh windows defined as shown \r\nbelow will not be recognized for these geometries. \r\n \r\nThe resulting mesh will appear as if mesh windows were never defined. \r\n----------------Page (1) Break----------------\r\n \r\nSolution \r\nThe mesh windows would work as expected if there were geometry facet corners \r\nin the region were the mesh windows were defined.  Depending on the CAD \r\npackage used, sometimes different .stl export settings can be used to generate \r\ngeometries with more/smaller facets. \r\nAlternatively, the following procedure can be used to create a geometry that will \r\nwork better for the mesh windows. \r\n1.  Generate a very fine surface mesh on the object.  You can use either Relative \r\nor Absolute mesh settings to do this.  Use as many elements as needed to \r\naccurately discretize the object.  If features of the geometry are being truncated \r\nor chopped off in the surface mesh, symmetry surfaces can be used (Geometry -\r\n-> Symmetric Surface) to mark the surfaces of these features so that this does \r\nnot occur.  Using more elements will also help preserve these features. \r\n2.  Once the fine surface mesh is generated (no need to solid mesh the object), \r\nclic--> Tools tab to save the surface mesh to a file \r\nsuch as FineSurfaceMesh.GEO. \r\n3.  Click on the Geometry button.  A window will pop up telling you that a solid \r\nmesh has not been generated yet.   \r\n4. Import FineSurfaceMesh.GEO as the geometry for the object. \r\n----------------Page (2) Break----------------\r\n5. Mesh the object.  The mesh windows that previously did not work will now be \r\nused as expected.  When defining the mesh windows, the icon in the \r\nWindow Definition dialog can be used to see the geometry location where the \r\nmesh windows will act. On the previous geometries, nothing highlighted when \r\ndoing this. \r\n \r\n  \r\n----------------Page (3) Break----------------\r\n \r\nRelated Information \r\nNone \r\n \r\n----------------Page (4) Break----------------\r\n"],"id":"1017"})